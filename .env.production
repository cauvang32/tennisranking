# 🎾 Tennis Ranking System - Production Environment
# This file contains production-specific settings

# Database Configuration (PostgreSQL)
DB_NAME=tennis_ranking
DB_USER=tennis_user
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
DB_HOST=localhost
DB_PORT=5432

# Admin Authentication (CHANGE THESE!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
ADMIN_EMAIL=admin@yourdomain.com

# Security Secrets (GENERATE NEW ONES!)
JWT_SECRET=CHANGE_THIS_JWT_SECRET_32_CHARACTERS_MINIMUM
CSRF_SECRET=CHANGE_THIS_CSRF_SECRET_32_CHARACTERS_MIN

# Server Configuration
PORT=3001
NODE_ENV=production

# Domain Configuration for Nginx Proxy Manager
# Set this to your actual domain where the app will be hosted
PUBLIC_DOMAIN=mydomain.com
BASE_PATH=/tennis
# Alternative configurations:
# For subdomain: PUBLIC_DOMAIN=tennis.mydomain.com, BASE_PATH=/
# For main domain: PUBLIC_DOMAIN=mydomain.com, BASE_PATH=/

# Rate Limiting Configuration (Production values)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
RATE_LIMIT_API_MAX=500

# CORS Settings (Production) - Update with your actual domain
ALLOWED_ORIGINS=https://mydomain.com,https://www.mydomain.com

# Nginx Proxy Manager Configuration
TRUST_PROXY=true
BEHIND_PROXY=true

# Security Configuration
FORCE_HTTPS=true
HSTS_MAX_AGE=31536000

# IMPORTANT SECURITY NOTES:
# 1. Change all passwords and secrets above
# 2. Use strong, randomly generated passwords
# 3. Never commit this file to version control
# 4. Keep this file secure and readable only by the application
